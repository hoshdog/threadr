# Secure production requirements for Threadr
# Includes additional security dependencies

# Core FastAPI requirements
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# HTTP client with security features
httpx==0.26.0
httpx[http2]==0.26.0  # HTTP/2 support

# Data validation
pydantic==2.5.3
email-validator==2.1.0

# Content processing
beautifulsoup4>=4.12.3
lxml>=5.1.0  # More secure HTML parser

# OpenAI integration
openai>=1.10.0

# Security dependencies
python-jose[cryptography]==3.3.0  # JWT support
passlib[bcrypt]==1.7.4  # Password hashing
python-decouple==3.8  # Environment variable management

# Redis for distributed rate limiting
redis==5.0.1
hiredis==2.3.2  # C parser for better performance

# Monitoring and logging
sentry-sdk[fastapi]==1.40.0
python-json-logger==2.0.7

# Security scanning
bandit==1.7.6  # Security linter
safety==3.0.1  # Dependency vulnerability scanner

# Testing security
pytest==7.4.4
pytest-asyncio==0.23.3
httpx-mock==0.2.0

# Additional security utilities
cryptography==41.0.7
secrets
hashlib
urllib3>=2.0.7  # Latest security patches

# Cloudflare integration (optional)
cloudflare==2.19.2

# Environment and configuration
python-dotenv>=1.0.0
pyyaml==6.0.1

# ASGI server (uvicorn only - no gunicorn)
# WARNING: Do not add gunicorn - it will conflict with Railway deployment