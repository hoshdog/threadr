# Stripe Payment Integration Configuration
# Copy this to your .env file and fill in your actual Stripe values

# Stripe API Keys (from Stripe Dashboard > Developers > API keys)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret_here

# Stripe Payment Link Configuration (optional - for direct payment links)
STRIPE_PRICE_ID=price_your_price_id_here

# Existing Threadr Configuration (keep these as they are)
PREMIUM_PRICE_USD=4.99
FREE_TIER_ENABLED=true
FREE_TIER_DAILY_LIMIT=5
FREE_TIER_MONTHLY_LIMIT=20

# Security (for webhook access)
API_KEYS=your_api_key_for_webhook_verification

# Instructions:
# 1. Get your Stripe keys from https://dashboard.stripe.com/apikeys
# 2. Create a webhook endpoint in Stripe pointing to: https://your-domain.com/api/webhooks/stripe
# 3. Select "checkout.session.completed" event
# 4. Copy the webhook signing secret to STRIPE_WEBHOOK_SECRET
# 5. Create a Payment Link in Stripe for $4.99/month and copy the price ID (optional)