# Threadr Backend Security Configuration Example
# Copy this file to .env and configure with your values

# Environment (development or production)
ENVIRONMENT=production

# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# API Authentication
# Multiple API keys supported, comma-separated
# Example: API_KEYS=key1,key2,key3
API_KEYS=your-secure-api-key-1,your-secure-api-key-2

# Rate Limiting Configuration
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW_HOURS=1

# URL Security - Domain Allowlist
# Comma-separated list of allowed domains for URL scraping
# Supports wildcards: *.example.com, blog.*.com
# Leave empty to allow all domains (NOT RECOMMENDED for production)
ALLOWED_DOMAINS=medium.com,*.medium.com,dev.to,*.dev.to,substack.com,*.substack.com,github.com,*.github.com

# CORS Origins (for production)
# Comma-separated list of allowed frontend origins
CORS_ORIGINS=https://threadr.vercel.app,https://threadr-frontend.vercel.app,https://www.threadr.app

# Redis Configuration (for caching and distributed rate limiting)
# Format: redis://user:password@host:port/db
# Format for Upstash: rediss://default:password@host:port
REDIS_URL=redis://localhost:6379/0

# Cache Configuration
CACHE_TTL_HOURS=24

# Content Limits
MAX_TWEET_LENGTH=280
MAX_CONTENT_LENGTH=10000

# Production Server Port
PORT=8000