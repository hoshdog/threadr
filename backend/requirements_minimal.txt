# Minimal requirements.txt for Railway FastAPI deployment testing
# Use this to isolate deployment issues by reducing dependencies to absolute minimum

# Core FastAPI requirements
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Basic request handling
httpx==0.26.0
pydantic==2.5.3

# Only if you need CORS (remove if causing issues)
python-multipart==0.0.6