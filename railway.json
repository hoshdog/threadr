{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Using railway.json override' && cd backend && pip install --upgrade pip && pip install -r requirements.txt && echo 'Verifying main_minimal.py' && ls -la src/main_minimal.py"
  },
  "deploy": {
    "startCommand": "cd backend && exec uvicorn src.main_minimal:app --host 0.0.0.0 --port $PORT --workers 1 --log-level info --access-log",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "region": "us-west1"
  },
  "environments": {
    "production": {
      "build": {
        "builder": "NIXPACKS"
      },
      "deploy": {
        "startCommand": "cd backend && exec uvicorn src.main_minimal:app --host 0.0.0.0 --port $PORT --workers 1 --log-level info"
      }
    }
  }
}